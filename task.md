AI智能分析系统：数据提取与核心算法调研和开发
数据关联建模与特征提取
定义针对不同层次的数据分组方法
将数据库数据提取模块包装成工具
瓶颈贡献度算法方案调研与测试
根因分析算法方案调研与测试
数值偏移监测算法方案调研与测试
多因素产能预测算法调研与测试
将各算法核心模块包装成工具
SOP 知识库与向量化工具 (RAG)
调研、确定开发框架（LlamaIndex 或 LangChain），配置环境
Markdown文本预处理与清洗
分割Markdown文档：将长篇文档切分成语义完整、长度合适的小文本块，用固定窗口切分，或根据 Markdown 标题进行语义切分
将文本块向量化，转化为高维稠密向量，存储到向量数据库
编写向量检索测试接口，根据用户提问召回 Top-N 最相关的文本块
Prompt 模板设计和LLM 路由整合，跑通全链路和测试、优化
知识库匹配度与召回率调试，与prompt调整
RAG优化：向量加关键词的混合检索，对召回的文本块重排，修改文本分块规则等
将RAG模块封装成工具，测试工具调用链路
搭建与前端接口，实现流式输出
LLM Agent 架构设计与 Prompt 微调
Agent 编排框架调研与选型
构建 Agent 核心执行循环（ReAct模式：思考-行动-观察训环）
工具描述说明书（JSON Schema）定义
改造、优化现有的系统提示词
编写大模型响应拦截与工具决策分发器
通过不同的语言输入与追问场景，调试工具与记忆的准确率，并调试，如编写并使用纠错工具
多轮对话、记忆与状态管理
构建符合工具调用（Tool Calling）规范的 Memory 消息队列
前后端接口通道开发与联调 (API)
调整模型提示词，输出符合前端大屏格式需求的数据
开发知识库增改工具
编写POST/PUT接口读取用户提问
编写 SSE 连接建立和流式推送通道接口
开发定时调度分析器，在查询SSE通道连接情况后推送回前端
系统数据迁移与 Docker 交付
数据导入/导出（跨厂迁移）功能开发和测试
编写 Dockerfile 与容器化打包
纯净环境一键部署与最终验收测试
调度与算法后端联调
前端UI界面组件开发
前端UI界面页面开发
前端联调测试

---

## 完成度评估

> 评估依据：仓库现有代码（`DataAgent/` 为主，RAG 部分见 `Agents/RAG_Agent.py`）。
> 图例：✅ 完成 ／ 🟡 部分完成 ／ ❌ 未实现

| # | 任务 | 完成度 | 实现位置 / 说明 |
|---|---|---|---|
| 1 | AI智能分析系统：数据提取与核心算法调研和开发 | 🟡 | 数据提取链路已完成，核心算法（瓶颈/根因/偏移/预测）未落地 |
| 2 | 数据关联建模与特征提取 | 🟡 | 数据模型见 [production_dictionary.md](DataAgent/domain/production_dictionary.md)，特征提取无独立代码，靠 SQL 在 prompt 中指导 |
| 3 | 定义针对不同层次的数据分组方法 | 🟡 | day/hour 窗口、GROUP BY 等口径写在字典第 3/6 章，无独立分组模块 |
| 4 | 将数据库数据提取模块包装成工具 | ✅ | [database.py](DataAgent/tools/database.py) `query_database` |
| 5 | 瓶颈贡献度算法方案调研与测试 | ❌ | 仅有 prompt 级分析口径（字典第 5 章），无算法实现 |
| 6 | 根因分析算法方案调研与测试 | ❌ | 仅有 abnormal_info 归因 prompt 指引，无算法实现 |
| 7 | 数值偏移监测算法方案调研与测试 | ❌ | 未实现 |
| 8 | 多因素产能预测算法调研与测试 | ❌ | 产量仅用「AMR 送料次数」近似（字典第 7 章），无预测算法 |
| 9 | 将各算法核心模块包装成工具 | ❌ | 无算法工具；tools/ 下仅 query_database / charts / executor / date / dictionary |
| 10 | SOP 知识库与向量化工具 (RAG) | ❌ | 仅有 [RAG_Agent.py](Agents/RAG_Agent.py) 的股票 PDF demo，非 SOP 知识库 |
| 11 | 调研、确定开发框架，配置环境 | ✅ | 选定 LangChain + LangGraph（DataAgent 与 RAG_Agent 均基于此） |
| 12 | Markdown 文本预处理与清洗 | ❌ | RAG_Agent 用 PyPDFLoader 处理 PDF，无 Markdown 预处理 |
| 13 | 分割 Markdown 文档 | 🟡 | 有 RecursiveCharacterTextSplitter 固定窗口切分，但针对 PDF、无标题语义切分 |
| 14 | 文本块向量化存储到向量库 | ✅ | [RAG_Agent.py](Agents/RAG_Agent.py) OllamaEmbeddings + Chroma（chroma.sqlite3） |
| 15 | 向量检索测试接口召回 Top-N | ✅ | [RAG_Agent.py](Agents/RAG_Agent.py) `as_retriever(k=5)` + `retriever_tool` |
| 16 | Prompt 模板与 LLM 路由整合、跑通全链路 | 🟡 | RAG 链路跑通（llm→retriever→llm），但知识库是股票 PDF 非 SOP，测试/优化缺失 |
| 17 | 知识库匹配度与召回率调试 | ❌ | 无评估与调试 |
| 18 | RAG 优化：混合检索/重排/分块规则 | ❌ | 仅纯 similarity 检索 |
| 19 | 将 RAG 模块封装成工具并测试链路 | 🟡 | RAG_Agent 有 retriever_tool，但未集成进 DataAgent 的 tools/ |
| 20 | 搭建前端接口实现流式输出 | ✅ | [app.py](DataAgent/app.py) + [index.html](DataAgent/static/index.html) SSE 逐 token |
| 21 | LLM Agent 架构设计与 Prompt 微调 | ✅ | [agent.py](DataAgent/agent.py) `SYSTEM_PROMPT` |
| 22 | Agent 编排框架调研与选型 | ✅ | 选定 LangGraph |
| 23 | 构建 Agent 核心执行循环（ReAct） | ✅ | model ⇄ tools 循环（`should_continue` + `call_tools`） |
| 24 | 工具描述说明书（JSON Schema）定义 | ✅ | @tool docstring + `convert_to_openai_tool` |
| 25 | 改造优化系统提示词 | ✅ | 同上 SYSTEM_PROMPT（含瓶颈/产量/时间范围专项指引） |
| 26 | 响应拦截与工具决策分发器 | ✅ | `should_continue` + `call_tools` |
| 27 | 调试工具与记忆准确率、纠错工具 | 🟡 | 工具返回错误供模型重试（隐式纠错），无显式纠错工具/系统化调试 |
| 28 | 多轮对话、记忆与状态管理 | ✅ | AsyncSqliteSaver + add_messages + session_id 隔离 |
| 29 | Tool Calling 规范的 Memory 消息队列 | ✅ | `convert_to_ollama`（tool_calls / tool_call_id / thinking） |
| 30 | 前后端接口通道开发与联调 (API) | ✅ | FastAPI `POST /chat` |
| 31 | 提示词输出符合前端大屏格式 | 🟡 | prompt 要求结构化中文结论，但无「大屏」JSON schema，前端为聊天页非大屏 |
| 32 | 开发知识库增改工具 | ❌ | domain_dictionary 只读，无增改 CRUD |
| 33 | POST/PUT 接口读取用户提问 | 🟡 | POST /chat 完成，无 PUT |
| 34 | SSE 连接建立与流式推送通道 | ✅ | `/chat` text/event-stream |
| 35 | 定时调度分析器（SSE 通道检查后推送） | ❌ | 未实现 |
| 36 | 系统数据迁移与 Docker 交付 | ❌ | — |
| 37 | 数据导入/导出（跨厂迁移） | ❌ | 未实现 |
| 38 | 编写 Dockerfile 与容器化打包 | ❌ | 仓库无 Dockerfile |
| 39 | 纯净环境一键部署与验收测试 | ❌ | 未实现 |
| 40 | 调度与算法后端联调 | ❌ | 未实现 |
| 41 | 前端 UI 组件开发 | 🟡 | 聊天组件（气泡/折叠思考/图表/lightbox）有，大屏组件无 |
| 42 | 前端 UI 页面开发 | 🟡 | 单个聊天页 index.html，无大屏/多页面 |
| 43 | 前端联调测试 | 🟡 | SSE 客户端可用，无正式联调测试 |

**统计**：✅ 完成 15 项 ／ 🟡 部分完成 12 项 ／ ❌ 未实现 16 项

**结论**：DataAgent 已把「数据提取 → 工具化 → ReAct Agent → 流式前端 → 记忆」主线跑通（第 4、20~30、34 项）。
两大块基本空白：① 核心算法（瓶颈贡献度/根因/偏移监测/产能预测，第 5~9 项）；② SOP RAG 与工程化交付（第 12、17、18、32、35、37~40 项）。
RAG 目前只有一份股票 PDF 的演示，不是任务要的 SOP 知识库。

---

## 工期解释（每个任务按 5 天估算的口径）

> **总账**：43 项 × 5 人天 = **215 人天**。单人约 43 周（≈10~11 个月）；2~3 人并行可压到 4~6 个月。
> **核心口径**：5 天是「兜底工期」而非「写 5 天代码」——含方案调研/设计、数据准备、编码实现、自测、跨模块联调、文档。算法与 RAG 类任务里「调研 + 评测」常占一半以上，这是最容易被人忽略、也最不能砍的部分。

| 任务分组 | 覆盖任务 # | 5 天的实际构成 | 为什么压缩不动（挑战回应） |
|---|---|---|---|
| 数据提取与核心算法 | 1–9 | 口径调研/建模(1.5) + 数据探查与特征(1) + 算法实现(1.5) + 离线评测(1) | 算法最怕「选型错→全返工」；评测要喂真实历史数据跑对比，缺一不可。四个算法（瓶颈/根因/偏移/预测）是四个独立课题，不是一份代码改改 |
| SOP 知识库 RAG | 10–19 | 框架选型(0.5) + 文档清洗/切分(1) + 向量化入库(0.5) + 检索+Prompt(1.5) + 召回调优/混合检索(1.5) | RAG 的瓶颈在「召回率调试」——切分粒度、检索方式、重排都要反复试，不是一版能到位；Markdown 清洗是纯脏活 |
| LLM Agent 架构 | 20–29 | 编排选型(0.5) + ReAct 循环/分发器(1.5) + JSON Schema/提示词(1.5) + 记忆/消息队列(1) + 场景调试(0.5) | 提示词和工具链路是「调」出来的不是「写」出来的；多语言追问、纠错场景要反复回归，这步省了上线就翻车 |
| 前后端接口与调度 | 30–35 | API/PUT(1) + SSE 通道(1) + 定时调度器(2) + 大屏格式联调(1) | 定时调度器要查 SSE 连接状态再推送，涉及后端状态机，不是加个接口那么简单 |
| Docker 交付 | 36–40 | 数据迁移工具(1.5) + Dockerfile/镜像(1.5) + 一键部署脚本(1) + 验收测试(1) | 跨厂数据迁移是最大雷——字段/枚举差异要清洗映射；纯净环境部署要反复验证，验收测试不能省 |
| 前端 UI | 41–43 | 组件开发(1.5) + 页面开发(2) + 联调测试(1.5) | 大屏可视化 + 图表实时刷新 + SSE 流式渲染，交互密集，UI 迭代天然慢 |

**关键反驳点——5 天不是一刀切**，实际按任务类型有弹性区间（5 天只是平均兜底）：

| 分组 | 实际工期区间 | 说明 |
|---|---|---|
| 核心算法 #5–8 | 5–10 天 | 最不可控，调研 + 评测吃时间 |
| RAG #11–19 | 3–8 天 | 召回调优波动大 |
| Agent #20–29 | 3–5 天 | 相对可控 |
| 接口 #30–35 | 3–7 天 | 调度器偏重 |
| Docker #37–39 | 3–6 天 | 数据迁移不定 |
| 前端 #41–43 | 4–8 天 | UI 迭代 |

**被砍价时的谈判要点**：
1. **算法评测不能砍**——评测砍了等于没做，上线没底气，后患更大。
2. **总工期不是简单累加**——算法(5–8) 与前端(41–43) 可并行，RAG 与 Agent 也可部分并行；5 天是「人天」，加人/并行能显著缩短日历工期。
3. **给出最小可行交付**——先交「数据提取 + ReAct + 流式前端」主线（#4、#20–30），算法与 RAG 分期、风险后置，用阶段性成果换信任，而不是一口吃成胖子。

---

## 未实现任务的实施方案（做什么 + 怎么做）

> 覆盖「完成度评估」里 ❌ 未实现的 16 项。技术栈复用现有 Python 环境，新增依赖：`scikit-learn` `xgboost` `lightgbm` `statsmodels` `prophet` `ruptures` `rank_bm25` `apscheduler`。

| # | 任务 | 做什么（交付物） | 怎么做（技术方案） |
|---|---|---|---|
| 5 | 瓶颈贡献度算法 | 产出「瓶颈贡献度排序报告 + 柱状图」 | 浪费时间占比分解（`_agg` 表按 `metrics_name` 聚合 `total_duration`）+ 失败贡献度（`t_prod_reports` 按 `abnormal_info` 聚合）+ Pareto 80/20 排序；可选熵权法合成综合分。包成 `analyze_bottleneck` 工具 |
| 6 | 根因分析算法 | 产出「根因规则 + 波动归因分解」 | 决策树 CART（max_depth=4，特征=device/action_type/hour/耗时分量，标签=`normal_end=0` 或「导航失败」）出可解释规则；送料次数波动用贡献度分解到异常数/设备数/工时/节拍。包成 `root_cause_analysis` 工具 |
| 7 | 数值偏移监测算法 | 产出「漂移/突变告警 + 控制图」 | SPC 控制图 CUSUM/EWMA（3σ 控制限）+ 稳健 Z-score(MAD) + STL 季节分解 + 变点检测(PELT)。包成 `detect_shift` 工具 |
| 8 | 多因素产能预测算法 | 产出「产能预测 + 置信区间 + 折线图」 | XGBoost/LightGBM（滞后 + 滚动特征）为主，Prophet(add_regressor) 做时序 baseline，多元线性回归做可解释 baseline；walk-forward CV，MAE/MAPE 评测。包成 `forecast_capacity` 工具 |
| 9 | 各算法核心模块包装成工具 | 4 个 `@tool` 落地进 `tools/` | 按现有 `@tool` + docstring 规范写 4 个工具，丢进 `tools/` 自动注册，`SYSTEM_PROMPT` 补一段用法说明 |
| 10 | SOP 知识库 RAG | 用 SOP Markdown 建库，替代股票 PDF demo | 在 DataAgent 的 `tools/` 新增 `rag.py`：Chroma + OllamaEmbeddings，文档源改为 SOP 目录下的 Markdown，替代 RAG_Agent 的 PyPDFLoader |
| 12 | Markdown 预处理与清洗 | 清洗脚本 | 去页眉页脚/空行/乱码、统一标题层级、表格转结构化文本、代码块保留；写成 `preprocess()` 供建库调用 |
| 17 | 召回率调试 | 评测集 + 指标 + 调参结论 | 人工标注 20~50 条 QA 对，算 recall@k / MRR / hit-rate；调 `chunk_size/overlap`、检索 top-k，迭代到达标 |
| 18 | RAG 优化（混合检索/重排/分块） | 混合检索 + 重排模块 | BM25（rank_bm25）关键词 + 向量相似度加权融合（RRF）；用 CrossEncoder rerank 模型重排；Markdown 按标题语义切分（MarkdownHeaderTextSplitter） |
| 32 | 知识库增改工具 | 增/改/删 SOP 条目 + 接口 | 提供 `add_doc/update_doc/delete_doc` 工具或 POST/PUT 接口；写入 Markdown 后增量更新/重建 Chroma 向量 |
| 35 | 定时调度分析器 | 定时任务 + 主动推送 | APScheduler 定时跑分析（如 `detect_shift`）；查询 SSE 通道连接状态，有结果主动推送前端（长连接或 WebSocket） |
| 36 | 数据迁移与 Docker 交付 | 整体交付包 | 统筹 #37~#39：迁移工具 + 容器 + 一键部署验收 |
| 37 | 数据导入/导出（跨厂迁移） | 导入/导出脚本 | pg_dump/pg_restore 或自定义导出（CSV/JSON）；跨厂字段映射 + 枚举值清洗，写成 `export_data/import_data` 工具 |
| 38 | Dockerfile 与容器化打包 | 容器镜像 + 编排 | 写 Dockerfile（基础镜像 + 依赖 + Noto 中文字体 + 启动命令）；docker-compose 编排 DB / agent / web |
| 39 | 一键部署与验收测试 | 部署脚本 + 验收清单 | `deploy.sh` 一键启动；写验收测试（服务启动/接口/SSE/算法/前端冒烟）逐项打勾 |
| 40 | 调度与算法后端联调 | 端到端联调 | 打通「定时触发算法 → 结果经 SSE 推前端 → 前端渲染」全链路，压测 + 回归 |

**依赖顺序**：先 #12→#10→#14/15（RAG 建库链）→ #17/#18（调优）；#5/#7（纯统计，快）→ #6 → #8（依赖历史数据量）；#9 在各算法完成后；#37→#38→#39→#36→#40（交付收尾）。

---

## 算法名词通俗解释

### #5 瓶颈贡献度

| 名词 | 是什么（大白话） | 用 AGV 数据的例子 |
|---|---|---|
| 占比分解 | 把「总浪费」拆成一块块，看谁占大头 | 全厂今天浪费 1000 小时（待料+待机+离线），AMR-03 一台占了 300 小时 → 贡献度 30% |
| Pareto 80/20（帕累托） | 按贡献度从大到小排序累加，抓「少数关键」 | 10 台设备里前 2 台的浪费加起来已占全厂 80%，重点就抓这 2 台 |
| 熵权法 | 多个指标合成一个综合分时，自动定权重：数据越有区分度的指标权重越大 | 各设备失败率都差不多（没区分度）→ 失败率权重低；浪费时间占比差异大 → 权重高 |

### #6 根因分析

| 名词 | 是什么（大白话） | 用 AGV 数据的例子 |
|---|---|---|
| 决策树 CART | 自动从数据学出一连串 if-else 规则 | 自动学出「if device=AMR-03 且 时段=14~16点 → 导航失败率 61%」；max_depth=4 表示最多 4 层，保证人看得懂 |
| 波动归因 / 贡献度分解 | 把「变化量」拆到每个因素头上 | 送料今天比昨天少 50 次 → 拆成「异常任务多了 20 次」「设备少了 2 台」「节拍慢了 5 秒」各自贡献了多少 |

### #7 数值偏移监测

| 名词 | 是什么（大白话） | 用 AGV 数据的例子 |
|---|---|---|
| SPC 控制图 | 画中心线 + 上下控制限，越界即报警（工厂质检经典工具） | 每天画送料次数，某天掉到控制限外 → 报警 |
| CUSUM 累积和 | 抓「小但持续」的偏移：把每次小偏差累加，正常时正负抵消、持续偏时越滚越大 | 待料时间每天悄悄涨一点，单看每天不明显，CUSUM 能早发现「整体在上飘」 |
| EWMA 指数加权移动平均 | 平滑曲线：最近的数权重大、越早越小，反映当前趋势 | 算近几天待料的加权平均画成线，看是否持续上行 |
| 3σ 控制限 | σ=标准差（衡量波动大小），正常数据超出「均值±3σ」概率不到 0.3%，算异常 | 送料次数历史均值 800、σ=40，掉到 680 以下（超 3σ）就报警 |
| Z-score / MAD | 衡量「今天偏离正常多少倍」；MAD 用中位数代替均值，更抗极端值干扰 | 今天送料 500，历史中位 800、MAD 100 → 偏离 3 个 MAD → 报警 |
| STL 季节分解 | 把曲线拆成「趋势+季节+残差」三块，只对残差做异常检测 | 产线每天 9 点开工 18 点收工本就有早晚周期，STL 先扣掉这周期，再看真正出乎意料的掉量 |
| PELT 变点检测 | 自动找出「规律从哪天开始变了」 | 产量从每天 800 平台性掉到 600，PELT 定位「变化在 7/15」，再去查那天发生了什么 |

### #8 多因素产能预测

| 名词 | 是什么（大白话） | 用 AGV 数据的例子 |
|---|---|---|
| 多元线性回归 | 找一条「直线公式」拟合：产量 = a×设备数 + b×待料占比 + c×异常率… | 系数直接说明「每多 1 台设备，产量涨多少」，最直观 |
| XGBoost / LightGBM（梯度提升树） | 很多棵决策树「接力」：前一棵错的地方下一棵专门补，最后求和；还输出「每个因素多重要」 | 预测明天送料次数，并告诉你「异常率」是影响最大的因素 |
| 滞后 / 滚动特征 | 把「过去的值」当输入，因为产量有惯性 | 预测明天，把「昨天产量」「上周同期」「近 7 天均值」也喂进去 |
| Prophet | Meta 开源的现成时序预测工具，自动处理趋势+季节+节假日，可加外生变量 | 比手搓回归省事，直接把设备数当外生变量塞进去 |
| walk-forward CV（滚动交叉验证） | 评估预测准不准的正确姿势：按时间「用过去猜未来」一步步前滚，绝不偷看未来 | 用 1~7 月训、预测 8 月；再用 1~8 月训、预测 9 月…… |
| MAE / MAPE | 衡量预测误差：MAE=平均绝对误差；MAPE=平均百分比误差，便于跨指标比较 | 预测 800 实际 820 → MAE=20；MAPE=20/820=2.4% |

### #18 RAG 优化（混合检索/重排）

| 名词 | 是什么（大白话） | 用 SOP 知识库的例子 |
|---|---|---|
| BM25 | 经典「关键词检索」打分：词出现多、且词本身少见 → 分高 | 查「待料时间」，SOP 里频繁出现「待料」的段落得分高 |
| 向量相似度 | 把文字变数字向量，语义相近的向量也相近，算距离 | 「待料时间」和「等料时长」字面不同但语义近，向量检索能找到 |
| RRF（倒数排名融合） | 把关键词检索 + 向量检索两个排名「合并」：名次取倒数求和 | 某段落向量排第 2、关键词排第 5 → 融合分 1/2+1/5=0.7，比单榜第 1 但另榜 100 的更靠谱 |
| CrossEncoder 重排 | 先粗召回 20 条，再用「精读模型」把「问题+段落」成对精确打分，重排取前 5 | 先捞 20 段，再精判哪 3 段真在回答「待料时间怎么优化」 |
| MarkdownHeaderTextSplitter | 按标题层级切分，保证每块语义完整，不硬按字数切 | 按「# 一级 / ## 二级标题」切，不会把「瓶颈分析口径」一章拦腰切断 |
